{
  "name": "k8s-eks-platform-api",
  "version": "1.0.0",
  "description": "REST API microservice for k8s-eks-platform",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage --coverageReporters=lcov --coverageReporters=text",
    "lint": "eslint src tests --ext .js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "pino": "^9.1.0",
    "pino-http": "^10.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": ["src/**/*.js"]
  },
  "engines": { "node": ">=20.0.0" }
}
